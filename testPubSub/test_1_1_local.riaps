//For initial capability Test 7.1.1.2.1
app testPubSub{
	message Topic1;

	component CompPub(logfile = "") {
		pub PubPort : Topic1;
		timer clock 1000;
	}

	component CompSub(logfile = "") {
		sub SubPort : Topic1;
	}


	actor ActorA(logfile = "ActorA") {
		//local Topic1;
		{
			CompA : CompPub(logfile = logfile);
		}
	}

	actor ActorB(logfile = "ActorB") {
		//local Topic1;
		{
			CompB : CompSub(logfile = logfile);
		}
	}
}
